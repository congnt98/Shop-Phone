import React from "react";

class Cart extends React.Component {
    render() {
        return (
            <>
                {this.props.children}
            </>
        )
    }
}
export default Cart